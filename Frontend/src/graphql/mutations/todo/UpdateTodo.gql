mutation UpdateTodo($id: ID!, $description: String!, $completion: Boolean!, $user_id: ID!) {
  updateTodo(id: $id, description: $description, completion: $completion, user_id: $user_id) {
    ...TodoFragment
  }
}
